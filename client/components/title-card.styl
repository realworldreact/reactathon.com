.title-card
  $grid('row', 'center')
  $center(40vw)
  background-size cover
  border 6px solid $primary
  color $secondary
  height 120px
  text-align center
  transform translateY(-50%)
  min-width 400px
  h1
    font-size 3.5rem
    flex-grow 1
    text-transform uppercase
